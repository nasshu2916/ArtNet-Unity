:root {
    flex-direction: row;
}

#UniverseSelector {
    width: 150px
}

UniverseInfo {
    height: 50px;
    margin: 2px;
    padding: 5px;
    align-items: flex-start;
}

UniverseInfo.selected {
    background-color: var(--unity-colors-highlight-background);
}

DmxViewer {
    flex-direction: row;
    flex-wrap: wrap;
    min-width: 1000px;
    margin: 5px;
}
